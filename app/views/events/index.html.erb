<h1>TO DO LIST</h1>













===============

<h2>Active Tasks</h2>

<table>
  <tr>
    <th>Contact Name </th>
    <th>Meeting Date</th>
    <th>Note</th>
    <th>Plan</th>
    <th>State</th>
    <th>Action</th>
  </tr>
  

  <% @active_events.each do |event|  %>
    <tr>
      <td><%= event.contact.combined_name rescue "" %></td>
      <td><!-- <%#= event.meeting_date.strftime("%m/%d/%Y") %> --></td>
      <td><%= event.notes %></td>
      <td><%= link_to event.plan, contacts_url %></td>
      <td><%= event.state %></td>
      <td><%= link_to "Update", edit_event_url(event) %></td>
    </tr>
  <% end %>

</table>

<br />

<h2>Inactive Events</h2>

<table>
  <tr>
    <th> Contact Name </th>
    <th> Meeting Date</th>
    <th> Note</th>
    <th> Plan</th>
    <th> State</th>
  </tr>
  

  <% @inactive_events.each do |event|  %>
    <tr>
      <td><%= event.contact.combined_name rescue "" %></td>
      <td><!-- <%#= event.meeting_date.strftime("%m/%d/%Y") %> --></td>
      <td><%= event.notes %></td>
      <td><%= event.plan %></td>
      <td><%= event.state %></td>
    </tr>
  <% end %>

</table>